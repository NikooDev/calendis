import React from 'react';
import type { Metadata } from 'next';
import { metadatas } from '@Calendis/utils/constants.util';

export const metadata: Metadata = metadatas('Calendis • Mentions légales', 'Retrouvez les mentions légales, les conditions d’utilisation et les informations relatives à la protection des données du site Calendis.');

const Page = () => {
	return (
		<>

		</>
	);
};

export default Page;